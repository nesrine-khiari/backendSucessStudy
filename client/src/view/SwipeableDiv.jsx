

const SwipeableDiv = ({ elements }) => {
  


  return (
    <div style={{ width: '100%' }} {...handlers}>
      <div style={{ width: '100%' }}>
        {elements[index]}
      </div>
      <div style={{ textAlign: 'center', marginTop: '10px' }}>
        
      </div>
    </div>
  );
};

export default SwipeableDiv;
